# Copy this file to `.env.local` for local development:
#
#   cp .env.example .env.local
#
# This file is intended for developer convenience only. In production, prefer
# configuring environment variables via your deployment platform and secret store.

# --- Policy behavior (PDP-specific) ---
#
# Policy modes: allow_all (default), deny_all, file
# ARP_POLICY_MODE=allow_all
#
# For file mode:
# ARP_POLICY_MODE=file
# ARP_POLICY_PATH=/path/to/policy.json

# --- Incoming auth (shared across ARP servers) ---
#
# If you set either ARP_AUTH_MODE or ARP_AUTH_PROFILE, the server will enable
# JWT auth middleware using these settings.
#
# Dev convenience: leave unset to run auth-disabled, or use:
# ARP_AUTH_PROFILE=dev-insecure
#
# For Keycloak local dev (requires ARP_STS_KeyCloak running):
# ARP_AUTH_PROFILE=dev-secure-keycloak
# ARP_AUTH_SERVICE_ID=arp-pdp
# ARP_AUTH_ISSUER=http://localhost:8080/realms/arp-dev
